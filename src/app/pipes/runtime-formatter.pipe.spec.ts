import { RuntimeFormatterPipe } from './runtime-formatter.pipe';

describe('RuntimeFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new RuntimeFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
